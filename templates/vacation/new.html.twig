{% extends 'base.html.twig' %}

{% block title %}New Vacation
{% endblock %}

{% block body %}
	{% if error %}
		<div class="alert alert-danger">{{ error }}</div>
	{% endif %}
	<h1>Create new Vacation</h1>

	{{ include('vacation/_form.html.twig') }}

	<a href="{{ path('vacation_index') }}">back to list</a>
{% endblock %}
