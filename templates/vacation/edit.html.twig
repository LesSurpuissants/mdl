{% extends 'base.html.twig' %}

{% block title %}Edit Vacation
{% endblock %}

{% block body %}
	{% if error %}
		<div class="alert alert-danger">{{ error }}</div>
	{% endif %}
	<h1>Edit Vacation</h1>

	{{ include('vacation/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('vacation_index') }}">back to list</a>
	<a href="{{ path('atelier_index') }}">Retour à la liste des atelier</a>

	{{ include('vacation/_delete_form.html.twig') }}
{% endblock %}
